
let count;
    function call(){
        let mars=document.getElementById("homew");
        let target = Math.floor(Math.random()*6)+1;
        console.log(target)
        mars.textContent=target;
    }
